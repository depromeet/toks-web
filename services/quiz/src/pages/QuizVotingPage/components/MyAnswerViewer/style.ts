import { theme } from '@depromeet/theme';
import styled from '@emotion/styled';

export const MyAnswerWrapper = styled.div`
  padding: 16px;
  background-color: ${theme.colors.gray110};
  border-radius: 16px;
  height: 30%;
  width: 50vw;
`;

export const MyAnswerContainer = styled.div`
  height: 100vh;
`;
